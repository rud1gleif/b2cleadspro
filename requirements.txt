# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0

# Database
sqlalchemy>=2.0.0
alembic>=1.13.0
asyncpg>=0.29.0          # async PostgreSQL driver (required by create_async_engine)
aiosqlite>=0.20.0        # async SQLite driver (local dev without Docker)

# HTTP client
httpx>=0.27.0

# HTML parsing
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Browser automation (JS-heavy scraping)
playwright>=1.44.0

# DNS resolution (MX record check)
dnspython>=2.6.0

# Redis queue
redis>=5.0.0

# Logging
loguru>=0.7.0

# Async
aiohttp>=3.9.0

# Email validation
email-validator>=2.1.0

# Optional: world cities dataset for location search
# pip install world-cities-cli  (or use bundled data)
